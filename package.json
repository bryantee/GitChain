{
  "name": "gitchain-node-capstone",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "npm run build:dev && node $npm_package_main",
    "test": "npm run build:dev && mocha",
    "clean": "rm -fr build",
    "build:dev": "npm run clean && mkdir build && cp -r src/* build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-preset-es2015": "^6.18.0",
    "chai": "^3.5.0",
    "chai-http": "^3.0.0",
    "mocha": "^3.1.2"
  },
  "dependencies": {
    "express": "^4.14.0"
  }
}
