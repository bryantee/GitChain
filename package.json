{
  "name": "gitchain-node-capstone",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "postinstall": "npm run build:dev",
    "start": "node $npm_package_main",
    "test": "npm run build:dev && mocha",
    "clean:dev": "rm -fr build",
    "build:dev": "npm run clean:dev && mkdir build && cp -r src/* build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-preset-es2015": "^6.18.0",
    "chai": "^3.5.0",
    "chai-http": "^3.0.0",
    "mocha": "^3.1.2"
  },
  "dependencies": {
    "body-parser": "^1.15.2",
    "express": "^4.14.0",
    "github_simplestat": "^6.0.0",
    "moment": "^2.17.0",
    "mongoose": "^4.7.0",
    "node-schedule": "^1.2.0"
  }
}
